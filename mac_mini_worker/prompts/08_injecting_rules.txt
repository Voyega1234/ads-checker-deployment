Use the following retrieved policy rules for this assessment.

RULES_BLOCK contains candidate policy rules retrieved from the database.
Some retrieved rules may be irrelevant to INPUT_MESSAGE.
Apply only rules that are applicable to the visible ad context, product/service category, claim type, platform context, or regulated activity.

Each rule in RULES_BLOCK is formatted as a labeled text block.

A rule may include:

* rule_index: short integer index for this prompt run
* rule_id: unique policy rule ID
* short_title: short human-readable rule title
* rule_text: the main compliance rule to assess against
* fix_note: practical fix guidance, when available
* source: human-readable source display
* locator: specific source location, page, section, or reference locator when available

Use only information actually present in RULES_BLOCK.
Do not invent missing rule metadata, legal references, source details, citations, sections, clause numbers, announcement numbers, or policy headings.

Use rule_text as the main authority for policy assessment.
Use short_title to help understand and name the policy issue.
Use fix_note only as guidance for a practical rewrite, not as an automatic replacement.
Use source and locator only for reference fields and display_reference.

For policy_check items, set rule_index to the matching RULES_BLOCK rule_index.
Do not create, guess, transform, shorten, or repair rule_id values yourself.

Do not create a policy failure merely because a rule appears in RULES_BLOCK.
A policy failure requires:

1. the rule is applicable to INPUT_MESSAGE, and
2. the visible INPUT_MESSAGE clearly violates that applicable rule.

=== RULES_BLOCK START ===
{{rules_block}}
=== RULES_BLOCK END ===
